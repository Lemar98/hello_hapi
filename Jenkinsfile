#!/usr/bin/env groovy

pipeline {
    
    agent { dockerfile true }
    
    stages {
        stage('Building...') {
            steps {
                echo "Success build!"
            }
        }
    }
}
